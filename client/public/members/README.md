Store board headshots here (e.g. `maya-r.jpg`). These assets are served at `/members/<filename>`.

Placeholder SVGs have been added so the layout renders even before real photos are uploaded. Replace each `.svg` with an actual image (ideally 800×800, JPG or PNG) using the same filename.

